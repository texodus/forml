

this is a comment

palette: →

    type X= Int
    type X = Int
    type X a= Int
    type X a = Int

    type F = Int->Int
    type F=Int->Int

    type G = Int -> Int -> Int

    type XXX = (Int -> Int)

   I'm a comment!

             -> Int

These are some tricky type signatures to parse

    type G=(Int→Int)→(Int→Int)
    type G=(Int->Int) | Int

    type H = { x : Int }
    type H = {x:Int,y:Str}

    type I = {x:Int}->{x:Int,y:Int}->(Int->{x:Int->Int,y:Int})->Int

    type X m a = X a b c -> m d e
    type Y m a = Y a (m b) -> Y a {x: m a}

These are union types

    type R = Str | Int
    type Z = Int

this is another comment
